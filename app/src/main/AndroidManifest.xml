<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fightcorona">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FightCorona">
        <activity
            android:name=".Login"
            android:label="Login"/>
        <activity
            android:name=".SymptomTracker"
            android:label="Symptoms"/>
        <activity
            android:name=".Myth"
            android:label="Myth"/>
        <activity
            android:name=".Awareness"
            android:label="Awareness"/>
        <activity
            android:name=".TestedPositive"
            android:label="Tested"/>
        <activity
            android:name=".TestedNegative"
            android:label="Not a Covid Patient"/>
        <activity
            android:name=".DailyUpdates"
            android:label="Updates"/>
        <activity
            android:name=".DetailedAnalysis"
            android:label="DetailedAnalysis"/>
        <activity
            android:name=".Analysis"
            android:label="DetailedAnalysis"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>